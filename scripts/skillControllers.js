tyrelSite.controller('emailController',
    ['$scope',
    function ($scope) {
        $scope.current = 1;
        $scope.setCurrent = function(x) {
            $scope.current = x;
        }
    }
]);
tyrelSite.controller('webController',
    ['$scope',
    function ($scope) {
        $scope.current = 1;
        $scope.setCurrent = function(x) {
            $scope.current = x;
        }
    }
]);
tyrelSite.controller('marketingController',
    ['$scope',
    function ($scope) {
        $scope.current = 1;
        $scope.setCurrent = function(x) {
            $scope.current = x;
        }
    }
]);